"""castiron — a schema→typed-code compiler for Python.

Point it at a schema source (a Supabase URL, your migrations, or a live DB) and
get typed models — and a typed client — with a ``check`` mode that fails CI when
your code drifts from the schema.
"""

__version__ = '0.6.0'
