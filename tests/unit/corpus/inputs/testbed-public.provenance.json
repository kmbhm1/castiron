{
  "origin": "captured",
  "source_repo": "github.com/kmbhm1/castiron-testbed",
  "seed_revision": "752649a",
  "postgrest_version": "14.14",
  "captured_with": "./scripts/capture.sh",
  "captured_on": "2026-08-09",
  "schema": "public",
  "definition_count": 26,
  "path_count": 43,
  "verified_by": "./scripts/verify.sh - clean-slate double rebuild, run-a and run-b byte-identical for all three documents and the pg_dump schema",
  "sha256": "c64be55ecee63f20dc641dbca4bb408ec6b8fb1461d589f26468147515fc262b",
  "notes": "Key-free: the API key travels in a header, not the body. host is 0.0.0.0:3000, so the document is port- and hostname-independent. The only environment-identifying field is info.version, which is the PostgREST version - useful provenance, not leakage. RE-CAPTURED 2026-08-09 at testbed 752649a (CI-140), which adds two VOLATILE probe_* functions to the seed - +2 /rpc/ paths, 41 -> 43, definitions unchanged and every pre-existing path byte-identical. probe_volatile_two_required measures the `required` array where it is the ONLY order signal (a VOLATILE function gets no GET operation); probe_volatile_all_optional is the ONLY shape in the corpus that produces ParameterOrder.UNKNOWN - >=2 arguments, all defaulted, so there is no GET and no `required` either. Before this capture that public enum member had no witness outside a synthetic unit document. tests/unit/sources/openapi/test_parse.py's TestTheArgumentOrderProbes asserts both here, so deleting or reordering the probes in a future capture goes red. The same testbed revision also adds edge.mangling_torture + edge.mangling_probe, which this document does NOT carry: the edge schema is deliberately never a corpus input (its document form is not contractual), so that witness lives in tests/integration/ only."
}
